services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vigil_dev
    ports:
      - "5432:5432"
    volumes:
      - vigil_db_data:/var/lib/postgresql/data

volumes:
  vigil_db_data:
